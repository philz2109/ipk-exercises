#include "canvas.hh"
#include <iostream>

int main(int argc, char *argv[]) { //Main-Funktion
	int horPixels = 10;
	int vertPixels = 8;
	//Canvas c(Point(500, 400), 1000, 800, vertPixels, horPixels);
	//Point p = c.coord(10, 8);
	//std::cout << p.getX() << "     " << p.getY() << std::endl;
	return(0);
}